apiVersion: mirror.openshift.io/v2alpha1
kind: ImageSetConfiguration
archiveSize: 15 #<- this will be the size of each tar. Change if needed
mirror:
  operators:
    - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.20
      packages:
       - name: rhbk-operator 
         defaultChannel: stable-v26.4 
         channels:
         - name: stable-v26.4
           minVersion: '26.4.11-opr.2'
           maxVersion: '26.4.11-opr.2'
